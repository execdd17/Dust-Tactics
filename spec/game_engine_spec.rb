require 'spec_helper'

describe DustTactics::GameEngine do

  before(:each) do
    @game_engine = GameEngine.new
  end

  it "should calculate who gets to place cover first" do
    pending
  end

end
